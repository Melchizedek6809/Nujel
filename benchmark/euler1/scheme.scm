(define (test-run)
  (let ((ret 0))
       (do ((i 0 (+ i 1)))
           ((>= i 10000000) ret)
         (if (or (zero? (modulo i 3))
                 (zero? (modulo i 5)))
             (set! ret (+ ret i))))))

(display "The sum is: ")
(display (test-run))
(newline)
